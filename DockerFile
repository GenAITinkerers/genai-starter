FROM python:3.10-slim

WORKDIR /app

# Copy project files
COPY setup.py setup.cfg README.md ./
COPY src ./src

# Install the package
RUN pip install --upgrade pip && pip install .

# Default command (can be overridden by docker-compose)
CMD ["python", "-m", "genai_start.101_text_embedding"]